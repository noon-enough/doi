<view class="wrapper">
    <record class="mt20"
            wx:if="{{show_modify_record}}"
            bind:submit="onSubmit"
            recode="{{record}}"
            place="{{place}}"
            status="{{status}}"
            posture="{{posture}}"></record>
</view>
