<view class="wrapper">
    <record
            wx:if="{{show_modify_record}}"
            bind:submit="onSubmit"
            class="mt20"
            recode="{{record}}"
            place="{{place}}"
            status="{{status}}"
            posture="{{posture}}" />
</view>
